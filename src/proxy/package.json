{
  "name": "@ghcp/proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "start:prod": "node dist/index.js",
    "test": "tsx --test src/accounts/*.test.ts src/copilot/*.test.ts src/db/*.test.ts src/routes/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@ghcp/shared": "0.1.0"
  }
}
